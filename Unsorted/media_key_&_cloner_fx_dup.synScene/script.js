var decimator = 0;
function update(dt) {
    
    setControlNormalized("meta/paused", _click.x);
    
    decimator++;
    if (decimator == 1){
        // print("normalized timer: " + particlesTimer.normalizedTimer);
        // print(isPaused);
        // printGlobalKeys()
    }

}